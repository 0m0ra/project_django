# Инструкция по коммиту кода в Git

## Вариант 1: Если Git уже установлен

### Шаг 1: Проверьте, установлен ли Git

Откройте PowerShell или командную строку и выполните:
```powershell
git --version
```

Если Git установлен, вы увидите версию (например, `git version 2.40.0`).

Если Git не установлен, перейдите к **Варианту 2**.

---

### Шаг 2: Инициализация репозитория (если еще не инициализирован)

```powershell
cd C:\Users\Zakhar\project
git init
```

---

### Шаг 3: Настройка Git (первый раз)

```powershell
git config --global user.name "Старцев Захар Вадимович"
git config --global user.email "onamo21@mail.ru"
```

---

### Шаг 4: Добавление файлов в репозиторий

```powershell
# Добавить все файлы
git add .

# Или добавить конкретные файлы
git add *.py
git add *.html
git add *.css
git add *.js
git add *.md
git add requirements.txt
```

---

### Шаг 5: Создание коммита

```powershell
git commit -m "Initial commit: To-Do List приложение на Django

- Реализовано управление задачами (CRUD)
- Добавлен календарь с привязкой задач к датам
- Реализована система аутентификации
- Добавлен раздел обратной связи
- Современный адаптивный интерфейс
- AJAX взаимодействие без перезагрузки"
```

---

### Шаг 6: Создание репозитория на GitHub (если еще нет)

1. Зайдите на https://github.com
2. Нажмите кнопку **"New"** (или **"+"** → **"New repository"**)
3. Введите название репозитория (например, `todo-list-django`)
4. Выберите **Public** или **Private**
5. **НЕ** ставьте галочки на "Initialize with README" (у нас уже есть файлы)
6. Нажмите **"Create repository"**

---

### Шаг 7: Подключение к удаленному репозиторию

```powershell
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Или через SSH (если настроен)
# git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO.git
```

---

### Шаг 8: Отправка кода на GitHub

```powershell
# Отправляем код в ветку main
git branch -M main
git push -u origin main
```

Если потребуется авторизация, GitHub попросит ввести логин и пароль (или токен).

---

## Вариант 2: Если Git не установлен

### Установка Git на Windows

1. **Скачайте Git:**
   - Перейдите на https://git-scm.com/download/win
   - Скачайте установщик для Windows

2. **Установите Git:**
   - Запустите установщик
   - Используйте настройки по умолчанию (просто нажимайте "Next")
   - После установки перезапустите PowerShell

3. **Проверьте установку:**
   ```powershell
   git --version
   ```

4. **После установки следуйте инструкциям из Варианта 1**

---

## Быстрая команда для коммита (если Git уже настроен)

```powershell
# Перейти в папку проекта
cd C:\Users\Zakhar\project

# Инициализировать репозиторий (если еще не инициализирован)
git init

# Настроить пользователя (если еще не настроено)
git config user.name "Старцев Захар Вадимович"
git config user.email "onamo21@mail.ru"

# Добавить все файлы
git add .

# Создать коммит
git commit -m "Initial commit: To-Do List приложение на Django"

# Подключить удаленный репозиторий (замените на свой)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Отправить на GitHub
git push -u origin main
```

---

## Что нужно добавить в .gitignore

Убедитесь, что файл `.gitignore` содержит:

```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
ENV/
env/

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
/media
/staticfiles

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment variables
.env
.env.local
```

---

## Полезные команды Git

```powershell
# Проверить статус файлов
git status

# Посмотреть изменения
git diff

# Посмотреть историю коммитов
git log

# Отменить изменения в файле
git checkout -- filename

# Посмотреть удаленные репозитории
git remote -v

# Обновить код с GitHub
git pull origin main
```

---

## Если возникли проблемы

### Проблема: "git: команда не найдена"
**Решение:** Установите Git (см. Вариант 2)

### Проблема: "Permission denied"
**Решение:** 
- Проверьте правильность URL репозитория
- Убедитесь, что вы авторизованы на GitHub
- Используйте Personal Access Token вместо пароля

### Проблема: "fatal: not a git repository"
**Решение:** Выполните `git init` в папке проекта

### Проблема: "Please tell me who you are"
**Решение:** Настройте имя и email:
```powershell
git config --global user.name "Ваше имя"
git config --global user.email "ваш@email.com"
```

---

## Альтернатива: GitHub Desktop

Если командная строка кажется сложной, можно использовать **GitHub Desktop**:

1. Скачайте: https://desktop.github.com/
2. Установите и войдите в аккаунт GitHub
3. Нажмите **"File"** → **"Add Local Repository"**
4. Выберите папку `C:\Users\Zakhar\project`
5. Нажмите **"Publish repository"** для отправки на GitHub

---

**Удачи с коммитом! 🚀**

